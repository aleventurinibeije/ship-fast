---
mode: agent
agent: design
description: 'Convert a design file (Figma, etc.) into structured component specifications.'
---

Design to spec: ${input:designInput:Design URL or file reference (e.g. https://www.figma.com/file/abc123/...)} ${input:ticketId:Ticket ID (optional — e.g. MYAPP-123)}

Follow the `design-to-spec` skill precisely.
