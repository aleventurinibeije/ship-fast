<!-- PLACEHOLDER — Copy a variant to activate this provider slot.

     Available variants (in .github/variants/providers/):
     - review-gitlab-mcp.md   — GitLab MRs via GitLab MCP
     - review-github.md       — GitHub PRs (DRAFT)

     Setup:
       cp .github/variants/providers/review-gitlab-mcp.md .github/context/providers/code-review.md

     Then fill in your project-specific constants (project ID, base URL, etc.)
-->
