---
mode: agent
agent: dev
description: 'Implement a ticket — read analysis doc, plan steps, write code, write tests.'
---

Implement ticket: ${input:ticketId:Ticket ID (e.g. MYAPP-123)}

Load the analysis doc if it exists. Break the work into a todo list. Implement and write tests following project conventions.
