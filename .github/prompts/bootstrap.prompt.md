---
mode: agent
agent: bootstrap
description: 'Configure .github/context/ for a new project — detects stack, collects constants, writes provider configs and scaffolds context files.'
---

Follow the `bootstrap` skill precisely. No input needed — the skill will scan the project and ask questions interactively.
